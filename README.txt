# Author: github.com/brendansturges
# Description: getDiskSpace.ps1 - built to query a file that the user chooses and queries for local disk names, free space, percent free & total drive size and exports to a txt file

